import { InicioFiltroPipe } from './inicio-filtro.pipe';

describe('InicioFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new InicioFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
